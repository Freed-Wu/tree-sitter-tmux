========================================
if-shell: single quotes
========================================

if-shell 'true' 'foo=bar'

---

(file
  (if_shell_directive
    (command)
    (str_single_quotes)
    (environment_assignment
      (name)
      (value))))

========================================
if-shell: block
========================================

if 'true' {
  foo=bar
}

---

(file
  (if_shell_directive
    (command)
    (str_single_quotes)
    (block
      (environment_assignment
        (name)
        (value)))))
