========================================
if-shell
========================================

if '[[ -d ~/.config/foo ]]' {
  foo=bar
}

---

(file
  (if_shell_directive
    (command)
    (raw_string_quote)
    (shell)
    (raw_string_quote)
    (block
      (environment_assignment
        (name)
        (value)))))
