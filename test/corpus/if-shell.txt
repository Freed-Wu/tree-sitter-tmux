========================================
if-shell: single quotes
========================================

if-shell 'true' 'var=1'

---

(file
  (if_shell_directive
    (command)
    (shell)
    (_statement)))

========================================
if-shell: single quotes else
========================================

if-shell 'true' 'var=1' 'var=2'

---

(file
  (if_shell_directive
    (command)
    (shell)
    (_statement)
    (_statement)))

========================================
if-shell: block
========================================

if 'true' {
  var=1
}

---

(file
  (if_shell_directive
    (command)
    (shell)
    (block
      (environment_assignment
        (variable_name)
        (value
          (string))))))

========================================
if-shell: block else
========================================

if 'true' {
  var=1
} {
  var=2
}

---

(file
  (if_shell_directive
    (command)
    (shell)
    (block
      (environment_assignment
        (variable_name)
        (value
          (string))))
    (block
      (environment_assignment
        (variable_name)
        (value
          (string))))))
