========================================
if-shell
========================================

if '[[ -d ~/.config/foo ]]' {
  foo=bar
}

---

(file
  (if_shell_directive
    (command)
    (str_single_quote)
    (shell)
    (str_single_quote)
    (block
      (environment_assignment
        (name)
        (value)))))
