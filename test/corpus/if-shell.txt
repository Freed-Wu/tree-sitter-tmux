========================================
if-shell
========================================

if '[[ -d ~/.config/foo ]]' {
  foo=bar
}

---

(file
  (if_shell_directive
    (command)
    (str_single_quotes)
    (block
      (environment_assignment
        (name)
        (value)))))
