========================================
if-shell
========================================

if '[[ -d ~/.config/foo ]]' {
  foo=bar
}

---

(file
  (if_shell_directive
    (command)
    (shell)
    (block
      (environment_assignment
        (name)
        (value)))))
