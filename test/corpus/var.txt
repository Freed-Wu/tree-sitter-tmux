========================================
variable single quote
========================================

set-option -g automatic-rename-format '1#{pane_title}2#{pane_current_path}3'

---

(file
  (set_option_directive
    (command)
    (command_line_option)
    (option)
    (value
      (str_single_quotes
        (expr_single_quotes
          (variable_name))
        (expr_single_quotes
          (variable_name))))))
