========================================
bind
========================================

bind v split-window -h

---

(file
  (bind_key_directive
    (command)
    (key
      (string))
    (split_window_directive
      (command)
      (command_line_option))))

========================================
bind: copy-mode
========================================

bind -T copy-mode-vi C-v send-keys -X rectangle-toggle v M-v

---

(file
  (bind_key_directive
    (command)
    (command_line_option)
    (key_table
      (string))
    (key
      (string))
    (send_keys_directive
      (command)
      (command_line_option)
      (key
        (string))
      (key
        (string))
      (key
        (string)))))

========================================
bind: \\
========================================

bind \\ split-window -h -c "#{pane_current_path}"

---
(file
  (bind_key_directive
    (command)
    (key
      (string))
    (split_window_directive
      (command)
      (command_line_option)
      (command_line_option)
      (start_directory
        (string)))))

========================================
bind: -
========================================

bind - split-window -v

---

(file
  (bind_key_directive
    (command)
    (key
      (string))
    (split_window_directive
      (command)
      (command_line_option))))

========================================
bind: -
========================================

bind - split-window -v

---

(file
  (bind_key_directive
    (command)
    (key
      (string))
    (split_window_directive
      (command)
      (command_line_option))))
