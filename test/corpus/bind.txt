========================================
bind \\
========================================

bind \\ split-window -h -c "#{pane_current_path}"

---

    (file
      (bind_key_directive
        (command)
        (key)
        (split_window_directive
          (command)
          (command_line_option)
          (command_line_option)
          (start_directory
            (string
              (variable
                (variable_name)))))))

========================================
bind send keys comment
========================================

bind a send-keys a # Foo

---

    (file
      (bind_key_directive
        (command)
        (key)
        (send_keys_directive
          (command)
          (key)
          (comment))))
