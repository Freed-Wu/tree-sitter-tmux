========================================
comment: start of line
========================================

# Foo

---

(file
  (comment
    (body)))

========================================
comment: no argument
========================================

bind # Foo

---

(file
  (ERROR
    (command))
  (comment
    (body)))

========================================
comment: missing argument
========================================

bind a # Foo

---

(file
  (bind_key_directive
    (command)
    (key
      (string))
    (comment
      (body))
    (attach_session_directive
      (MISSING command))))

========================================
comment: end of line
========================================

bind a send-keys a # Foo

---

(file
  (bind_key_directive
    (command)
    (key
      (string))
    (send_keys_directive
      (command)
      (key
        (string))))
  (comment
    (body)))
