========================================
comment: start of line
========================================

# Foo

---

(file
  (comment))

========================================
comment: no argument
========================================

bind # Foo

---

(file
  (ERROR
    (command))
  (comment))

========================================
comment: missing argument
========================================

bind a # Foo

---

(file
  (bind_key_directive
    (command)
    (key
      (string))
    (comment)
    (attach_session_directive
      (MISSING command))))

========================================
comment: end of line
========================================

bind a send-keys a # Foo

---

(file
  (bind_key_directive
    (command)
    (key
      (string))
    (send_keys_directive
      (command)
      (key
        (string))))
  (comment))
