========================================
comment no argument
========================================

bind # Foo

---

    (file
      (ERROR
        (command))
      (comment))

========================================
comment missing argument
========================================

bind a # Foo

---

    (file
      (ERROR
        (command))
      (comment))

========================================
comment
========================================

bind a send-keys a # Foo

---

    (file
      (bind_key_directive
        (command)
        (key)
        (send_keys_directive
          (command)
          (key)
          (comment))))
